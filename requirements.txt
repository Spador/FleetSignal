# pandas and pyarrow are used only to write parquet at the bronze layer.
# the generation logic itself stays pure stdlib Python for explainability.
pandas
pyarrow

# duckdb is the warehouse engine: it runs the SQL transforms on parquet, no server to host.
duckdb

# pytest runs the unit tests for the anomaly-detection functions.
pytest
